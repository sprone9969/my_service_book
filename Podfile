# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'my service book' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

   pod 'RZTransitions'

  target 'my service bookTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'my service bookUITests' do
    # Pods for testing
  end

end
